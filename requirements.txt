# Core
streamlit>=1.54
streamlit-local-storage>=0.0.25
pandas>=2
python-dotenv>=1

# LLM providers (lazily imported — only needed if you have the API key)
anthropic>=0.79
openai>=2.20
google-genai>=1.63

# Document parsing (only needed for external response import)
python-docx>=1.1
pymupdf>=1.25
